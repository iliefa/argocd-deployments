apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../base
namePrefix: test-
secretGenerator:
    - name: secret
      literals:
      - DB_URL='test_url'
      - DB_USER='test_user' 
commonLabels:
  env: test